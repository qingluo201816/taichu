# 独立校验报告

规格：`1.1/通用写作智能体评测体系重构`

模式：`requirements`

校验时间：`2026-07-26T16:49:37.963Z`

目标对象：`.sdd/specs/1.1/通用写作智能体评测体系重构/requirements.md`

目标 SHA-256：`fcd02e25286df181d81ebba88e0fa5f17d2895acebe9e03d4f107a8c40e26a76`

discovery：`.sdd/specs/1.1/通用写作智能体评测体系重构/validation-discovery-requirements.md`

discovery SHA-256：`574001aab0dc202d36bd87b7b3d51ea9ae5ea0a65dda0db9b5c13fa02545a487`

Git/工作树基线：`master@82bab37a5514f8a6f4d632872010293a910c2bec`；工作树在校验开始前已有大量用户与并行任务改动。目标、discovery 与本报告均为未跟踪文件，本次只写 discovery 和本报告，未把其他改动纳入结论。

## 结论摘要

目标文档已完整覆盖隔离执行、硬门禁优先、失败归因、四态工作记忆专项、DeepSeek V4 Pro 首轮闭环、破坏式清理、桌面工作台和数据边界，296 条验收标准的编号与 EARS 句式机械检查也全部通过。

但仍有 4 项 major 缺口：生产能力目录没有形成“全部能力必须被覆盖”的可执行闭环；合成脚本化基线缺少严格消费与漂移失败契约；系统问题持久化失败没有明确阻断闭环；多模型比较没有固定预算与环境，也没有把 fallback 污染、实际模型身份不符和关键证据缺失明确排除在排名之外。这些缺口均可让不完整或不可比的结果获得正式结论，当前对象不能通过需求门禁。

## 独立发现范围与方法

1. 阶段一未读取 `requirements.md`、旧 discovery 或旧报告，仅读取 `spec.json.description`、根与局部规则、当前源码、测试、配置、活动资料以及 `pico-v3` 的固定基准参考。
2. 阶段一使用 `rg --files`、`rg -n`、精确源码阅读和哈希检查形成 E1—E20 预期清单，并先写入非空 discovery。
3. discovery 落盘后才计算目标首次 SHA-256 并完整读取目标共 899 行；随后按原始目标、项目不变量、当前真实资产和 E1—E20 逐项对照。
4. 项目规则明确禁用 Graphify，本次未读取或查询任何 Graphify 产物。
5. 目标首次读取前与报告写入前的 SHA-256 均为 `fcd02e25286df181d81ebba88e0fa5f17d2895acebe9e03d4f107a8c40e26a76`，对象未发生变化。

## 匹配项

| # | 独立预期 | 目标覆盖 | 证据 |
|---|---|---|---|
| 1 | 新评测验证已设计能力，现有运行监控仅作为关联证据 | 明确区分被评对象、审计证据与相邻评测 | `requirements.md:12-16`、`:91-112`、`11.1-11.15`；`service.py:81-103` 证明旧评测只是既有 run 的事后评分 |
| 2 | 密封快照、案例干净副本、作者活动事实隔离 | 完整覆盖快照身份、隔离副本、越界失败与事实前后核对 | `requirements.md:174-250` |
| 3 | 硬门禁先于指标，缺证据、无效或未完成不得通过 | 六类案例硬门禁、套件硬门禁与机制硬门禁均明确，分数只能解释 | `requirements.md:274-332`、`:574-590` |
| 4 | 失败需结构化分类并保留并发失败 | 同时保存主要类别、完整类别集合、确定性优先级与证据 | `requirements.md:334-396` |
| 5 | 套件、案例、尝试和证据包可追溯 | 使用稳定标识、内容哈希和只读引用，禁止 mtime 猜配 | `requirements.md:398-428` |
| 6 | 机制实验仅在真实开关和可比条件下成立 | 无真实控制时只给专项硬门禁；不可比实验不得报告增益 | `requirements.md:430-492` |
| 7 | 工作记忆四态及全部污染入口分别受硬门禁约束 | `ACTIVE`、`STALE`、`REJECTED`、`SUPERSEDED`、依赖传播、修复引用、并行隔离和投影污染均有可观察条件 | `requirements.md:712-756`；当前四态事实见 `src/taichu/domain/agent_memory/models.py:37-43` |
| 8 | DeepSeek V4 Pro 先完成真实全套件及问题闭环 | 明确禁止闭环前开展或发布多模型比较 | `requirements.md:758-812`；当前默认模型见 `.env.example:6,14-19`、`src/taichu/infrastructure/llm/model_catalog.py:49-61` |
| 9 | 系统问题使用唯一 API、八字段和 todo/processed 语义 | 创建、去重、超时复查、八字段顺序、关闭条件均被覆盖 | `requirements.md:774-792`、`:810-812`；当前校验契约见 `src/taichu/application/services/mvp_inbox_service.py:377-412` |
| 10 | 旧评测破坏式替换，同时保留相邻共享资产与历史快照 | 精确列出后端、前端、测试、旧结果和资料清理，并保护恢复、运行监控、知识抽取与召回评测 | `requirements.md:628-674` |

## 错误项

未发现目标把不存在对象误写为“现有”的事实错误；列出的旧评测路径、路由、前端入口、工作记忆四态、DeepSeek V4 Pro 配置与收件箱 API 均能在当前源码中复核。

## 遗漏项

| ID | 遗漏 | 为什么影响正确性 | 目标与源码证据 | 严重性 |
|---|---|---|---|---|
| M-01 | 缺少“当前生产能力目录中的每项能力都必须被基准覆盖，否则套件预检失败”的双向完备条件，也未要求把能力目录身份/快照绑定到套件。 | `1.2` 只约束案例指向能力，`1.3` 只要求展示映射，`1.8/1.17` 只拒绝未知能力；即使遗漏大量已注册能力，现有验收条件仍可成立并给出“整体能力结论”。 | `requirements.md:138-172`、`:324-332`；当前机器可读目录有 29 个评测 profile，且测试要求与生产发现一致：`src/taichu/application/evaluations/capability_profiles.py:32-208`、`tests/unit/application/evaluations/test_capability_profiles.py:13-25`。 | major |
| M-02 | 合成脚本化确定性基线缺少严格执行契约：未要求逐步匹配预期能力/模型交互，未声明乱序、意外调用、脚本耗尽、脚本未消费完必须失败，也未要求相同脚本重复运行产生规范化一致的 Runtime 结果。 | `1.14` 只把脚本绑定到案例身份，`4.1-4.8` 只保证对既有工件的检查确定；一个宽松替身仍可吞掉行为漂移，无法作为真实 Runtime 的确定性回归基线。 | 原始目标 `requirements.md:6`、范围 `:77`、`1.14`、`4.1-4.8`、`8.10-8.12`；参考资产的脚本客户端及耗尽边界见 `pico-v3/pico/testing.py:6-27`。 | major |
| M-03 | 未声明系统问题 API 明确失败、格式校验失败或最终无法持久化时必须阻断该轮缺陷闭环，并要求套件工件与问题记录形成可复核的双向关联。 | `15.10` 只处理“超时或结果不确定”。若创建返回确定失败，后续修复与完整复跑仍可能让 `15.19` 的闭环状态被解释为完成，却没有项目规定的问题记录和关联证据。 | `requirements.md:774-800`；唯一入口和严格八字段校验见 `src/taichu/api/routes/inbox.py:203-246`、`src/taichu/application/services/mvp_inbox_service.py:146-164,377-412`，格式回归见 `tests/integration/api/test_mvp_first_api.py:330-380`。 | major |
| M-04 | 多模型准入的固定条件没有包含案例资源预算和运行环境身份；也未明确要求实际 provider/model 身份与请求目标一致，并把 provider probe 失败、fallback/replay 污染和关键身份/用量证据缺失排除出排名，而不是按低分计入。 | `15.20` 仅固定代码、套件、夹具、案例、能力、授权和解码，`15.21` 依赖未完整定义的“固定可比条件”。当前网关存在提供商探测、fallback 与 replay 路径，缺少显式准入会产生表面同套件、实际执行条件不同的排名。 | `requirements.md:802-806`，对比 `2.5`、`2.10-2.13`、`5.10-5.12`、`8.14-8.16`；当前 provider/fallback/replay 边界见 `src/taichu/infrastructure/llm/rightcode.py:65-152,161-235,1378-1441`。 | major |

## 多余项

未发现无上游依据且实质扩大产品范围的要求。第 12 节列出具体清理路径属于原始“破坏式替换”目标的可验证边界；第 13—15 节分别服务安全校验、工作记忆专项和真实模型闭环，不属于旁支功能。

## 不可验证或规则冲突

- 不可验证：M-01 至 M-04 均缺少足以机械判定的条件，详见上表。
- 规则冲突：未发现。
- 虚构现有接口、类、字段、文件或调用链：未发现。
- 过期证据：未发现；目标两次哈希一致。

## 需求/设计/任务追踪表

| discovery 预期 | 目标映射 | 结果 |
|---|---|---|
| E1—E4：评测对象、破坏式替换、冻结快照、隔离执行 | 范围 3、需求 2、3、11、12 | 匹配 |
| E5：严格脚本化确定性执行基线 | 需求 1.14、4、8.10—8.12 | 部分覆盖，M-02 |
| E6—E9：硬门禁、失败解释、证据工件 | 需求 5—7 | 匹配 |
| E10：生产能力目录全覆盖 | 需求 1 | 部分覆盖，M-01 |
| E11—E13：真实机制实验、四态记忆、DeepSeek 首轮 | 需求 8、14、15.1—15.5 | 匹配 |
| E14：系统问题唯一入口与不可绕过闭环 | 需求 15.6—15.19 | 部分覆盖，M-03 |
| E15—E16：闭环后多模型可比准入 | 需求 15.19—15.22 | 部分覆盖，M-04 |
| E17—E19：桌面页面、恢复路径、派生数据边界 | 需求 9—11、13 | 匹配 |
| E20：数字 ID、EARS 与机械可观察性 | 全文验收标准 | 句式和编号匹配；四项核心条件仍不可机械判定 |

## 测试与机械检查

| 命令/检查 | 结果 |
|---|---|
| `Get-FileHash requirements.md -Algorithm SHA256`（首次读取前、报告写入前） | 两次均为 `fcd02e25286df181d81ebba88e0fa5f17d2895acebe9e03d4f107a8c40e26a76` |
| `Get-FileHash validation-discovery-requirements.md -Algorithm SHA256` | `574001aab0dc202d36bd87b7b3d51ea9ae5ea0a65dda0db9b5c13fa02545a487`，文件 20,843 字节，非空 |
| PowerShell 验收 ID/EARS 扫描 | 15 个需求章节、296 条验收标准、重复 ID 0、编号缺口 0、非 EARS 前缀 0 |
| `rg -n` 对硬门禁、四态记忆、DeepSeek、问题入口、多模型、清理范围的定向扫描 | 关键主题均存在；M-01—M-04 的缺失条件未找到 |
| 当前源码、测试、配置静态复核 | 旧评测形态、29 个能力 profile、四态记忆、DeepSeek 配置、问题格式和 provider/fallback 边界均已复核 |
| 实现测试 | 本轮是需求文档独立校验且未修改业务代码；未运行实现测试。FAIL 由需求级 major 遗漏直接成立。 |

## 分级问题

### Critical

无。

### Major

1. M-01：生产能力目录没有形成全覆盖硬门禁。
2. M-02：合成脚本基线没有严格交互消费与漂移失败语义。
3. M-03：系统问题持久化失败没有阻断闭环，也缺少双向关联硬条件。
4. M-04：多模型比较缺少预算、环境、实际模型身份与 fallback/证据污染准入规则。

### Minor

无。

### Info

- 文档已有很好的硬门禁优先级、异常表与追踪摘要；修复上述四个合同缺口即可重新执行同一独立门禁。

## 修正项（FAIL 时）

1. 在需求 1 增加机器可读能力目录快照、`能力 -> 至少一个合格案例` 的全覆盖条件，以及任一当前生产能力未覆盖或目录身份漂移时套件预检失败。
2. 在需求 4 增加严格脚本运行语义：按序匹配、意外/乱序调用、脚本耗尽、剩余步骤均失败，并要求相同输入、脚本和 Runtime 配置重复执行得到规范化一致结果。
3. 在需求 15 增加系统问题创建/更新确定失败、格式拒绝或重试后仍未持久化时闭环保持未完成；要求问题记录与套件/轮次/失败证据稳定双向关联且可读回验证。
4. 扩展多模型准入条件：固定案例预算与运行环境身份；记录并核验请求模型、实际 provider/model、probe 和 fallback/replay 状态；任何 fallback 污染、身份不符或关键证据缺失均标为不可比并排除排名。

## 门禁理由

独立需求门禁要求 critical 与 major 均为 0，并要求核心范围可机械验证。当前 4 项 major 会分别允许“能力未全覆盖”“合成基线漏报行为漂移”“系统缺陷无问题记录仍闭环”或“不可比模型进入排名”，均直接影响评测结论可信性，因此不能通过。

结论：FAIL
