# 正文知识沉淀 Agent 方案

> 更新日期：2026-07-04

## 1. 文档目的

本文档只讨论太初的第一个真实 Agent：**正文知识沉淀 Agent**。

它的目标不是写作页续写，不是 RAG 问答，也不是图谱推理，而是把 Markdown 正文中的有效信息抽取成可审核的知识库草稿或更新建议，最终由作者确认后进入结构化知识库主数据。

本文是讨论稿，用于反复迭代方案，不是最终实现说明。

## 2. 当前核心结论

正文知识沉淀 Agent 是太初第一个真正适合 LangGraph 的 Agent。

原因是它不是单轮问答，而是一个长流程任务：读取正文、按章节处理、调用真实 LLM 抽取、类型归类、字段填充、匹配已有知识卡、生成草稿或补丁、校验、冲突检测、人工确认、写入知识库。

第一版不做 RAG，不做向量库，不做图谱，不做 GraphRAG。那些是未来规划，不属于这个 Agent 的第一阶段。

这个 Agent 的输出不应直接写入有效知识库，而是先进入待确认中间态。作者确认后，才写入 MongoDB 主数据。

推荐定位：

```text
正文知识沉淀 Agent = 正文 → 候选知识卡 / 更新建议 → 人工确认 → MongoDB 有效知识库
```

不推荐叫“自动写知识库 Agent”，因为它听起来像 LLM 可以绕过作者确认直接污染事实源。

## 3. 用户观点整理

当前只讨论第一个 Agent，不扩散到写作页 AI、RAG、图谱、向量库、ES。

功能入口应该放到“智能体工作台”页面。当前已有智能体工作台只是示例，后续实现时应删除示例内容，不做旧示例兼容。

评测必须同步做。需要看清每个节点的提取状态和指标，不能只看到最终结果。

当前章、当前卷、全书的工作流本质相近，因为底层都是按章节这个小单元抽取。区别主要是任务范围、批处理调度、批内去重、阶段汇总、进度展示。

当前卷和全书任务会产生更多重复候选，因此需要在正式知识库比对之前增加批次内去重和合并。

这个任务实时性要求不高。它是作者写完正文后自动更新知识库的后台任务，不是写作过程中等着即时回答的功能。

用户希望自动化更多，但同时需要准确率。因此不能只做保守的手动录入，也不能让 LLM 直接无审核写入有效知识库。

用户认为多智能体可能有价值，尤其是为了达到更好的抽取效果。但多智能体不应该变成每个类型 Agent 从头读一遍正文，因为那会浪费成本。更合理的方式是前置节点统一读取正文、形成共享候选和摘要，再分发给不同类型的子 Agent 或节点处理。

## 4. 数据层建议

当前推荐分层：

```text
Markdown 正文
  ↓
JSON 中间态：待确认候选卡、更新建议、冲突项、运行记录
  ↓
MongoDB 主数据：作者确认后的角色卡、地点卡、事件卡等结构化知识
  ↓
未来：Qdrant 向量层、ES 全文索引、Neo4j/事件层/GraphRAG
```

正文继续存 Markdown。

知识卡有效主数据存 MongoDB，用于结构化查询和写作页知识参考。

抽取 Agent 生成的候选内容先放在中间态。第一版可以用 JSON 文件承载中间态，便于调试、回放、删除和人工审核。后续如果候选量变大、审核页复杂，再考虑把中间态迁入 MongoDB 的 staging 集合。

中间态不是有效知识库。它可以校验宽松，可以包含不确定字段，可以保存候选文本和置信度，但不能被写作页当作已确认知识使用。

## 5. 为什么第一版不做 RAG

正文知识沉淀 Agent 的输入就是正文。它不需要先检索再增强。

RAG 的典型场景是：用户问一个问题，系统先从知识库或正文里检索材料，再交给 LLM 整理回答。

正文知识沉淀 Agent 的场景是：系统批量读取正文，从正文中抽取结构化事实。

所以第一版的重点是：

```text
读取正文 → 抽取候选知识 → 生成草稿 / 更新建议 → 人工确认
```

不是：

```text
用户提问 → 检索 → 增强 → 回答
```

Qdrant、ES、Neo4j、GraphRAG 都是未来层。现在只需要把来源引用、章节 ID、知识卡 ID、运行记录、候选项这些字段设计好，为未来 RAG 留接口。

## 6. 智能体工作台页面定位

推荐把正文知识沉淀 Agent 放在“智能体工作台”页面里，而不是新增一个主导航页面。

智能体工作台应该从示例页改造成真实 Agent 工作台。当前示例内容后续实现时删除，不做兼容。

推荐页面结构：

```text
智能体工作台
  左侧：Agent 列表
    - 正文知识沉淀 Agent
    - 未来其他 Agent

  中间：当前 Agent 工作区
    - 任务配置
    - 运行进度
    - 节点状态
    - 候选结果

  右侧或下方：评测与运行详情
    - 指标
    - 节点日志
    - 提示词快照
    - 错误信息
```

对于第一个 Agent，页面内建议分为四个 Tab：

```text
运行任务
待处理候选
运行详情
评测指标
```

不建议新开“评测主页面”。评测是 Agent 运行的一部分，第一版放在智能体工作台内部更清楚。未来如果 Agent 很多、评测复杂，再考虑单独的全局评测中心。

## 7. 处理范围设计

当前章、当前卷、全书都可以抽象成同一套工作流，只是输入范围不同。

底层处理单元建议始终是章节。

```text
当前章 = 1 个章节任务
当前卷 = 多个章节任务 + 批次级合并
全书 = 多个卷 / 多个章节任务 + 更大的批次调度
```

第一版不建议直接开放全书抽取作为默认入口。

推荐开放顺序：

```text
A0：当前章节
A1：自定义章节范围，例如 1～5 章
A2：当前卷
A3：全书，作为高级批量任务后置
```

原因：当前章节最适合调提示词和字段；自定义章节范围适合测试重复抽取和合并；当前卷适合验证批量调度；全书会放大成本、错误、重复、冲突和审核压力。

## 8. 推荐 LangGraph 工作流

第一版推荐使用“主图 + 类型子节点”的结构，而不是每个类型 Agent 从头读一遍正文。

推荐流程：

```text
Start
  ↓
LoadScopeNode
  读取任务范围：当前章节 / 章节范围 / 当前卷

  ↓
ChapterIteratorNode
  按章节分发处理，每章带 chapter_id、display_title、正文 hash

  ↓
PreprocessChapterNode
  清理输入、保留段落、生成章节处理片段

  ↓
GeneralExtractionNode（LLM）
  从章节中抽出候选实体、候选事实、候选事件

  ↓
TypeDispatchNode
  按候选类型分发到角色、境界、功法、地点、势力、物品、规则、事件等类型填充节点

  ↓
TypeSpecificFillNode（部分 LLM）
  按类型字段契约 v2 填充草稿字段

  ↓
NormalizeNode
  规范字段、枚举、空值、章节引用、来源摘录

  ↓
BatchDeduplicateNode
  当前批次内去重、合并同名或疑似同一实体

  ↓
MatchExistingKnowledgeNode
  和 MongoDB 已确认知识卡做匹配，判断新卡、更新旧卡、冲突

  ↓
BuildReviewItemsNode
  生成待作者处理的候选新卡、候选更新、候选冲突、建议忽略

  ↓
HumanReviewNode
  作者确认、编辑、废弃、合并

  ↓
WriteToKnowledgeBaseNode
  写入 MongoDB 主数据或保留中间态
```

## 9. 多智能体取舍

用户倾向自动化更多，并希望最终效果更好，因此可以引入多智能体思想。

但第一版不推荐“角色 Agent、地点 Agent、事件 Agent、势力 Agent 各自从头读完整正文”。这样成本高、重复多、合并难，也会让同一章被多次消耗。

推荐方案是“共享正文理解 + 类型专家填表”：

```text
一个通用抽取节点先读正文，形成共享候选池。
然后按类型分发给类型专家节点。
类型专家节点只处理候选和必要上下文，不重新从头读全文。
```

这样既保留了多 Agent 的专业化，又控制了成本和重复。

第一版可先实现：

```text
通用抽取器
角色填表器
地点/势力/物品/事件填表器
规则/境界/功法填表器
统一合并与校验器
```

不一定一开始就每种知识类型一个独立 Agent。

## 10. 哪些节点需要 LLM

判断标准：如果任务是确定性的、可校验的、可写死的，就不要用 LLM；如果任务需要理解语义、归纳、消歧、压缩、判断类型，可以用 LLM。

不需要 LLM 的节点：

```text
读取 Markdown
获取章节列表
计算正文 hash
按章节迭代
保存运行记录
字段 schema 校验
枚举值合法性检查
来源摘录长度裁剪
写入 JSON 中间态
写入 MongoDB 主数据
按 id 查询已有知识卡
状态流转
```

需要 LLM 的节点：

```text
从正文抽取候选实体和事实
判断候选知识类型
生成角色身份摘要
生成人物关系摘要
生成事件描述
概括规则例外
从上下文判断 role_type、faction_type、item_type、technique_type
生成更新建议
解释冲突原因
```

混合节点：

```text
实体匹配：先规则匹配名称和别名，再用 LLM 判断疑似同一对象
冲突检测：先程序比较字段，再用 LLM 解释冲突文本
批次去重：先规则去重，再用 LLM 判断模糊重复
```

## 11. 自动化与准确率策略

第一版目标不是少人工，而是让人工只处理高价值确认。

推荐把候选分成四类：

```text
候选新卡
候选更新
候选冲突
建议忽略
```

每条候选必须包含：

```text
候选类型
目标知识类型
候选内容
来源章节
原文摘录
置信度
是否匹配已有知识卡
冲突说明
建议动作
```

作者确认入口：

```text
确认入库
编辑后确认
合并到已有卡
废弃
稍后处理
```

不要开放“全部自动写为有效知识”。

可以后续开放“高置信度批量确认”，但必须有来源、无冲突、通过 schema 校验。

## 12. 评测与仪表盘

评测必须和 Agent 同步做。

第一版评测不评判文学质量，也不评估模型回答风格，而是评估抽取链路和候选质量。

运行级指标：

```text
运行 ID
任务范围
章节数
成功章节数
失败章节数
候选总数
候选新卡数
候选更新数
候选冲突数
废弃数
确认数
平均每章候选数
平均每章耗时
总耗时
模型名称
prompt 版本
schema 版本
```

节点级状态：

```text
节点名称
状态：待执行 / 执行中 / 成功 / 失败 / 跳过
开始时间
结束时间
耗时
输入摘要
输出摘要
错误信息
重试次数
```

候选级指标：

```text
候选类型
知识类型
置信度
是否有来源摘录
schema 校验结果
是否命中已有卡
是否冲突
作者处理状态
```

评测页面建议放在智能体工作台内，而不是单独主页面。可以作为当前 Agent 的“评测指标”Tab。

## 13. 是否引入 LangSmith

第一版不建议把 LangSmith 作为硬依赖。

推荐先做内部运行记录和仪表盘，因为这些数据和产品强相关，需要展示给作者，也需要和候选审核、知识卡写入绑定。

LangSmith 可以作为开发调试工具接入，用于查看 LLM 调用链、提示词、token、耗时和错误，但不能替代产品内的运行记录。

第一版推荐：

```text
产品内必须有 run 记录、节点状态、候选状态、指标面板。
LangSmith 可选接入，不作为 MVP 阻塞项。
```

## 14. 增量更新思路

章节修改后，不应该重建全书。

每章需要保存：

```text
chapter_id
content_hash
last_extracted_hash
last_extracted_at
extract_schema_version
prompt_version
model_name
```

如果正文 hash 未变化，跳过抽取。

如果正文 hash 变化，只重跑该章节或包含该章节的任务范围。

已确认知识卡不因正文变化自动删除或覆盖。系统只生成新的候选更新或提示来源可能过期。

来源引用如果依赖的章节发生变化，可以标记为“来源可能已过期”，等待作者复核。

## 15. 第一版最小可用目标

第一版正文知识沉淀 Agent 的最小可用状态：

```text
能在智能体工作台选择当前章节或小范围章节
能读取 Markdown 正文
能调用真实 LLM 生成候选知识
能按类型字段契约 v2 输出候选草稿
能生成来源引用
能展示节点状态和运行指标
能进入待作者处理区域
作者能确认、编辑、废弃候选
确认后能写入 MongoDB 有效知识库
```

不要求：

```text
全书一次性稳定抽取
RAG 问答
向量检索
ES 全文检索
图谱构建
GraphRAG
复杂人物关系图谱
秘密与动机字段
写作页 AI 真实效果评测
```

## 16. 推荐实施阶段

阶段 0：删除智能体工作台示例内容，建立真实 Agent 工作台壳子。

阶段 1：实现运行任务页面，支持选择当前章节、小范围章节，创建运行记录。

阶段 2：实现 LangGraph 主图骨架，先用假 LLM 走完整节点和状态记录。

阶段 3：接入真实 LLM 到通用抽取节点，输出候选 JSON。

阶段 4：实现类型填表节点，按类型字段契约 v2 生成草稿。

阶段 5：实现批次去重、匹配已有知识卡、冲突检测。

阶段 6：实现待处理候选审核区，确认后写入 MongoDB。

阶段 7：实现评测指标和节点状态仪表盘。

阶段 8：实现增量抽取与来源过期提示。

## 17. 当前未决问题

1. 第一版开放范围到底是“当前章节 + 自定义章节范围”，还是直接开放“当前章节 + 当前卷”？

推荐：先开放当前章节和自定义章节范围，当前卷作为第二步。

2. JSON 中间态第一版是否一定放外部文件，还是候选运行记录也放 MongoDB staging 集合？

推荐：先外部 JSON，运行记录和前端展示可通过 API 读取；后续候选量变大再迁入 MongoDB staging。

3. 多智能体第一版拆到什么粒度？

推荐：不要每个类型一个完整 Agent；先做通用抽取器 + 类型专家填表节点。

4. LangSmith 是否第一版接入？

推荐：可选，不作为 MVP 阻塞。产品内运行记录和仪表盘必须自建。

5. 确认后的知识卡是否立即进入写作页可参考知识库？

推荐：是。只要状态为有效，就可以被写作页结构化查询使用。

6. 批量抽取运行中是否允许中断和恢复？

推荐：当前章节任务可以不做恢复；当前卷或多章节任务需要至少支持失败章节重试。

7. 是否需要单独的“待处理候选”主页面？

推荐：第一版放在智能体工作台当前 Agent 内，不新增主页面。

## 18. 当前推荐总方案

正文知识沉淀 Agent 第一版采用“智能体工作台内的 LangGraph 批处理工作流”。

它以 Markdown 正文为输入，以 JSON 中间态为候选输出，以 MongoDB 有效知识库为最终主数据。

它不上 RAG、不上向量库、不上图谱。它的核心价值是把正文中的角色、境界、功法、地点、势力、物品、规则、事件等信息自动整理成可审核知识，减少作者手动维护知识库的负担。

第一版应追求“可控、可追溯、可评测”，而不是追求完全自动写入。

关键原则：

```text
LLM 负责理解和建议。
程序负责校验和状态流转。
作者负责最终确认。
MongoDB 只保存确认后的主数据。
JSON 中间态保存未确认候选。
评测仪表盘和节点状态从第一版同步建设。
```
