# Codex 总提示词

更新日期：2026-07-03

把下面内容复制给 Codex。

```text
你现在是 qingluo201816/taichu 仓库的 Coding Agent。请执行“太初知识库字段重构任务包”。

这是一个知识库字段重构任务，不是泛泛讨论产品。你必须严格以仓库和任务包为准。

你必须先读取：

1. AGENTS.md
2. docs/rule.md
3. TAICHU_DESIGN.md
4. 产品文档/MVP功能改版.md
5. 产品文档/MVP迭代规则与LLM边界补充.md
6. 产品文档/知识库字段重构任务包/00-任务包总览.md
7. 产品文档/知识库字段重构任务包/01-阶段任务.md
8. 产品文档/知识库字段重构任务包/02-字段契约.md
9. 产品文档/知识库字段重构任务包/03-类型字段契约.md
10. 产品文档/知识库字段重构任务包/04-schema注册表契约.md
11. 产品文档/知识库字段重构任务包/05-数据清理规则.md
12. 产品文档/知识库字段重构任务包/06-验收清单.md
13. 产品文档/知识库字段重构任务包/07-证据返回模板.md

最高优先级规则：

当前已有知识库卡片全部清理，不保留旧数据，不迁移旧结构，不保留旧信息。旧 confirmed 卡、旧 worldbuilding 规则卡、旧 foreshadow 卡、旧 source_refs 结构都不要兼容。这个规则需要写入项目实际使用的 Coding Agent 规则文档。

本任务目标：

1. 清空当前 project_assets/source/knowledge/ 中已有旧知识 JSON 文件。
2. 删除第一版中的伏笔 foreshadow 类型。
3. 统一新知识卡通用字段。
4. 把 source_refs 改为 source_note + source_origin。
5. 建立后端 schema 注册表。
6. 前端知识库编辑界面基于后端 schema 渲染。
7. 所有章节引用字段保存章节稳定 ID，不保存展示序号。
8. 所有后端字段名使用正式英文名，前端和作者可见描述使用中文。

不要做：

1. 不接真实 LLM。
2. 不做真实 RAG 效果验证。
3. 不做 AI 自动正文抽取真实能力。
4. 不做复杂关系图谱。
5. 不做伏笔卡兼容。
6. 不做旧知识卡迁移。
7. 不恢复旧 source_refs。
8. 不修改入口页视觉。
9. 不重写写作编辑器主体功能。

执行阶段：

请只执行用户指定阶段。如果用户没有指定阶段，只执行 Phase 0：Preflight / 现状确认与规则写入。

每个阶段完成后必须停止，按 07-证据返回模板.md 返回证据，等待用户确认。不得自动进入下一阶段。不得在停机点前自动 commit。

如果发现仓库现状与任务包冲突，以任务包为准，但必须在证据中写明冲突点。

如果某个极小实现细节未定，按任务包中的“实现默认”执行，不要阻塞任务。

现在请先执行 Phase 0：Preflight / 现状确认与规则写入。
```
