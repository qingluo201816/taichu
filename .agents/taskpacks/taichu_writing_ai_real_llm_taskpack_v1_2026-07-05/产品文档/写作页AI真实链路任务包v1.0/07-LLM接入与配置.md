# LLM 接入与配置

更新日期：2026-07-05

## 要求

生产路径必须真实调用 LLM。优先复用当前仓库已有 LLM factory / provider 配置，不新增平行配置体系。

## 配置缺失

当 API Key、Base URL、模型名缺失：

1. 不返回假输出。
2. 不前端拼“模拟输出”。
3. 生成 failed run。
4. 保存错误信息。
5. 前端显示中文错误：“当前未配置可用模型，无法调用真实 LLM。”

## 测试

单元测试可使用 test double，但只能在测试目录或测试配置中使用。生产入口不得走 test double。
