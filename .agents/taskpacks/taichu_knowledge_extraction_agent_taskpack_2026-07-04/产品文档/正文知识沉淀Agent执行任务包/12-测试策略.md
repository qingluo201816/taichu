# 测试策略

> 更新日期：2026-07-04

## 1. 后端单元测试

必须覆盖：

```text
AgentRun JSON 读写
文件名格式
run status 校验
node status 校验
llm_calls 保存完整 prompt / response
review_items 状态流转
KnowledgeRepository list_active_cards
KnowledgeRepository search_active_identity
draft/deprecated 不参与匹配
create_active_card 写入 active
patch_active_card 不覆盖已有非空字段
source_note 追加
候选字段白名单校验
禁止 fields/source_refs/body/tags/confidence
```

## 2. LangGraph 测试

使用 mock LLM 覆盖：

```text
短章节整章处理
长章节分段处理
GeneralExtractionNode JSON 解析成功
GeneralExtractionNode 非 JSON 失败
CharacterExpertNode 不输出禁用字段
EntityExpertNode 不编造 ID
NormalizeAndValidateNode 拦截错误字段
内部重复合并
active 卡匹配
draft/deprecated 不匹配
WriteIntermediateJsonNode 写完整 run 文件
```

## 3. API 测试

覆盖：

```text
创建运行
运行列表
运行详情
候选列表
confirm create_card
confirm update_card
conflict 直接 confirm 被拒绝
ignore 直接 confirm 被拒绝
edit-confirm
reject
defer
中文错误响应
```

## 4. 前端测试 / 手动验证

覆盖：

```text
/agent-workbench 可访问
Agent 列表显示正文知识沉淀 Agent
四个 Tab 正确
选择章节后可以开始抽取
运行列表刷新
候选中文状态显示
英文枚举不暴露
prompt/response 折叠展开
确认入库操作
编辑后确认操作
废弃和稍后处理
评测指标展示
```

## 5. 全量命令

```text
uv run python -m unittest discover tests
uv run ruff check .
uv run mypy
cd web && npm run test:editor
cd web && npm run lint
cd web && npm run build
```

如果仓库当前脚本有变化，以实际 package.json 和 pyproject.toml 为准，但必须在证据中说明替代命令。
