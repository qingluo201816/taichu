# 03-证据返回模板

> 更新日期：2026-06-30

Codex 每个阶段完成后必须按本模板返回。不得只写“已完成”。

## 1. 阶段信息

```text
阶段：
执行范围：
是否进入下一阶段：否
是否自动提交 commit：否
```

## 2. 完成情况

| 目标 | 状态 | 说明 |
|---|---|---|
| 目标 1 | 完成 / 部分完成 / 未完成 | 说明 |
| 目标 2 | 完成 / 部分完成 / 未完成 | 说明 |

## 3. 修改文件

| 文件 | 类型 | 说明 |
|---|---|---|
| `path/to/file` | 新增 / 修改 / 删除 | 说明 |

## 4. 数据结构 / API 变更

```text
对象名：
中文含义：
核心字段：
状态流转：
存储位置：
```

```text
方法：
路径：
用途：
请求字段：
响应字段：
错误情况：
```

## 5. 用户界面变更

```text
涉及页面：
新增中文文案：
移除中文文案：
确认未出现英文内部标识：是 / 否
```

## 6. AI 链路证据

仅涉及 AI 阶段填写。

```text
是否使用真实 LLM：否
是否生成提示词快照：是 / 否
是否显示“模拟输出”：是 / 否
是否只检查输出结构、链路和提示词快照：是 / 否
是否做了输出质量评测：否
```

## 7. 验收命令

列出实际执行命令和结果。

```text
命令：
结果：通过 / 失败
说明：
失败是否与本阶段有关：是 / 否 / 不确定
```

建议命令：

```bash
uv run python -m unittest discover tests
uv run ruff check .
uv run mypy
cd web && npm run test:editor
cd web && npm run lint
cd web && npm run build
```

## 8. 手动验收步骤

```text
1. 打开页面：
2. 点击：
3. 输入：
4. 保存：
5. 刷新：
6. 预期结果：
```

## 9. 风险和未完成项

| 项目 | 风险 | 建议 |
|---|---|---|
| 示例 | 示例 | 示例 |

## 10. 停机声明

必须写：

```text
本阶段已到停机点。未自动进入下一阶段，未自动提交 commit。等待作者确认。
```
