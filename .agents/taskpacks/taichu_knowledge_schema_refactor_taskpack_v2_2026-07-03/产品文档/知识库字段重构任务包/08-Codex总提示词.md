更新日期：2026-07-03


# Codex 总提示词

你现在是 qingluo201816/taichu 仓库的 Coding Agent。请执行「知识库字段重构任务包 v2」。

这是一次强约束的知识库字段重构，不是兼容旧数据，不是迁移旧结构，不是补字段。用户已经明确：以本任务包和最新图片中的修改意见为准。

你必须先阅读：

- `产品文档/知识库字段重构任务包/00-任务包总览.md`
- `产品文档/知识库字段重构任务包/01-纠错与最终决策.md`
- `产品文档/知识库字段重构任务包/02-字段契约.md`
- `产品文档/知识库字段重构任务包/03-类型字段契约.md`
- `产品文档/知识库字段重构任务包/04-schema注册表契约.md`
- `产品文档/知识库字段重构任务包/05-阶段任务.md`
- `产品文档/知识库字段重构任务包/06-验收清单.md`
- `产品文档/知识库字段重构任务包/07-证据返回模板.md`
- `产品文档/知识库字段重构任务包/09-禁止事项.md`
- `AGENTS.md`
- `CLAUDE.md`，如果存在
- `docs/rule.md`
- 当前知识库相关后端、前端和数据目录

如果旧代码、旧文档、旧测试与本任务包冲突，以本任务包为准。

## 必须执行的核心规则

1. 当前旧知识卡全部清空，不迁移，不保留旧结构。
2. 必须把「旧知识卡不保留、不迁移、不兼容」写入实际 Coding Agent 规则文档。
3. 知识卡第一版不维护 `body`。
4. 知识卡第一版不维护 `tags`。
5. 知识卡第一版不维护 `fields`。
6. 知识卡第一版不维护 `confidence`。
7. 知识卡第一版废弃 `source_refs`，使用 `source_origin` + `source_note`。
8. 知识卡第一版不维护 `relations`。
9. 第一版删除 `foreshadow` 类型。
10. 角色卡第一版不维护 `personality`、`motivation`、`appearance`。
11. 所有后端字段 key 使用正式英文名。
12. 所有作者可见字段名、按钮、提示、说明使用中文。
13. 类型专属字段直接存为卡片顶层字段，不使用 `fields` 包裹对象。
14. 所有章节引用字段保存稳定 `chapter_id`，不保存展示用章节序号。

## 第一阶段只执行 Phase 0

首次执行时，请只执行：

Phase 0：Preflight / 规则写入与现状确认

不要清理数据，不要改模型，不要改 API，不要改前端业务代码。

Phase 0 完成后按证据模板返回，然后停止等待用户确认。

## 阶段 commit 规则

每阶段可以一个 commit，但必须在阶段完成、测试通过、返回证据并由用户确认后再 commit。不得在停机点前自动提交。

## 禁止行为

不要说“隐藏或高级区展示 body/tags/fields/confidence”。这是错误方向。
不要保留旧 `source_refs`。
不要保留 `foreshadow`。
不要保留角色性格、动机、外貌字段。
不要做旧结构兼容层。
不要迁移旧 worldbuilding 卡片。
不要把内部枚举值直接展示给用户。
