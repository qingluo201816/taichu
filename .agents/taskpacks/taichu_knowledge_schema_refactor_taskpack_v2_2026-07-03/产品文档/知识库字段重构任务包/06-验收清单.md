更新日期：2026-07-03


# 验收清单

## 数据清理验收

- [ ] `project_assets/source/knowledge/` 下旧 JSON 已按任务清理。
- [ ] 旧 `worldbuilding` 卡片不保留。
- [ ] 旧 `foreshadow` 卡片不保留。
- [ ] 新目录只保留支持类型：character、realm、technique、location、faction、item、rule、event。
- [ ] 旧结构不做迁移脚本。
- [ ] 旧结构不做兼容读取。

## 字段验收

- [ ] 新知识卡 JSON 不含 `body`。
- [ ] 新知识卡 JSON 不含 `tags`。
- [ ] 新知识卡 JSON 不含 `fields`。
- [ ] 新知识卡 JSON 不含 `confidence`。
- [ ] 新知识卡 JSON 不含 `source_refs`。
- [ ] 新知识卡 JSON 不含 `relations`。
- [ ] 角色卡不含 `personality`。
- [ ] 角色卡不含 `motivation`。
- [ ] 角色卡不含 `appearance`。
- [ ] 伏笔类型 `foreshadow` 不可创建。
- [ ] 类型专属字段直接作为卡片顶层字段保存。

## 通用字段验收

- [ ] 新知识卡包含 `id`。
- [ ] 新知识卡包含 `type`。
- [ ] 新知识卡包含 `name`。
- [ ] 新知识卡包含 `aliases`。
- [ ] 新知识卡包含 `summary`。
- [ ] 新知识卡包含 `importance`。
- [ ] 新知识卡包含 `status`。
- [ ] 新知识卡包含 `source_origin`。
- [ ] 新知识卡包含 `source_note`。
- [ ] 新知识卡包含 `created_at`。
- [ ] 新知识卡包含 `updated_at`。

## 状态验收

- [ ] 新建知识卡默认为 `draft`。
- [ ] 允许标记为 `active`。
- [ ] 允许标记为 `deprecated`。
- [ ] 不再使用 `confirmed`。
- [ ] 废弃卡默认隐藏。
- [ ] 草稿不参与 AI 检索或证据回答。
- [ ] 标记有效时必须校验 `name`、`summary`、`source_origin`、`source_note`。

## 来源验收

- [ ] 草稿可以没有来源。
- [ ] 标记有效时必须有 `source_origin`。
- [ ] 标记有效时必须有 `source_note`。
- [ ] `source_origin` 支持 `inbox_fact`、`agent_extract`、`manual`。
- [ ] `agent_extract` 第一版只预留，不实现自动提取。
- [ ] 前端展示中文来源方式。
- [ ] 前端不展示内部枚举给普通用户。

## schema 注册表验收

- [ ] 后端有知识卡 schema 注册表。
- [ ] schema 支持 8 类知识卡。
- [ ] schema 不包含 foreshadow。
- [ ] schema 不包含被禁止字段。
- [ ] 前端表单按 schema 渲染，不把类型字段写死在 UI。
- [ ] schema 字段含中文 label。
- [ ] schema 字段含 field_type。
- [ ] schema 字段含 required_when_active。
- [ ] schema 字段含 display_group。
- [ ] schema 字段含 list_display。
- [ ] schema 字段含 ai_usage。

## 前端验收

- [ ] 类型导航无「伏笔」。
- [ ] 前端不出现 `body`、`tags`、`fields`、`confidence`、`source_refs`、`relations` 的维护入口。
- [ ] 角色卡表单不出现性格、动机、外貌。
- [ ] 前端字段展示为中文。
- [ ] 普通用户不可见内部枚举值。
- [ ] 通用字段优先展示：名称、别名、摘要、重要程度、来源说明。
- [ ] 创建知识卡时先选类型。
- [ ] 首次保存后才写入后端。
- [ ] 类型为空时显示中文空状态。

## 测试验收

- [ ] 后端测试覆盖创建 8 类知识卡。
- [ ] 后端测试覆盖禁止 foreshadow。
- [ ] 后端测试覆盖禁止字段不可出现在输出中。
- [ ] 后端测试覆盖草稿转有效校验。
- [ ] 后端测试覆盖废弃状态。
- [ ] 前端测试或手动验收覆盖 schema 渲染。
- [ ] 所有可用 lint/build/test 通过，或说明已有无关失败。
