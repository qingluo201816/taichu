# 证据返回模板

> 更新日期：2026-06-27

Codex 每个 Phase 完成后必须按以下格式返回。

## Phase

本次执行：Phase X：xxx

## 设计依据

- 已读取 `TAICHU_DESIGN.md`：是/否
- 已读取 `STYLE_LOCK.md`：是/否
- 本次对应设计规则：列出引用的小节或 token

## 修改文件

| 文件 | 类型 | 说明 |
|---|---|---|
| path | 新增/修改/删除 | 说明 |

## 未修改范围

明确说明未修改：

- 后端 `src/taichu/**`
- `project_assets/**`
- API / 数据结构 / 状态机
- 非本 Phase 页面或组件

## 实现内容

简述本 Phase 完成的实际内容。

## Token / 样式变化

列出新增或修改的 token、全局样式、组件样式。

## 页面覆盖

列出本次涉及页面和手动访问路径。

## 检查结果

```text
cd web
npm run lint
结果：...

npm run build
结果：...

npm run test:editor
结果：...（如适用）
```

如果失败，说明是否为本任务引入、失败原因、下一步建议。

## 手动验收步骤

1. 打开 xxx。
2. 检查 xxx。
3. 执行 xxx。

## 风险与遗留

列出未完成项、风险、需要用户决策的地方。

## 是否可以进入下一 Phase

是/否，并说明理由。
