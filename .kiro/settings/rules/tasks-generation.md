# 任务生成规则

## 格式

任务文件标题固定为：

```markdown
# 实现计划
```

任务使用复选框：

```markdown
- [ ] 1. 任务标题
  - 需求来源：需求 1
  - 边界范围：...
  - 验证：...
```

## 要求

- 任务必须可独立执行和验收。
- 每个任务必须标注需求来源。
- 每个任务必须标注边界范围。
- 每个实现任务必须配套测试或验证任务。
- 不得把无关重构放进任务清单。
- 不得引入未在设计文档中说明的新依赖或新架构。
