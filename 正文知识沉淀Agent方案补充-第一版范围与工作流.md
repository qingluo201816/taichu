# 正文知识沉淀 Agent 方案补充：第一版范围与工作流

> 更新日期：2026-07-04

## 1. 本文目的

本文补充《正文知识沉淀Agent方案.md》，重点修正和确认以下问题：

1. “按章节小单元抽取”是否准确。
2. 第一版开放抽取范围。
3. JSON 中间态存储方式。
4. 多智能体拆分粒度。
5. LangGraph 的第一版设计重点。
6. 评测与仪表盘如何同步建设。

本文仍然只讨论第一个真实 Agent：正文知识沉淀 Agent，不讨论写作页 RAG、不讨论向量库、不讨论图谱。

## 2. 对“按章节小单元抽取”的修正

原表述“当前章、当前卷、全书的工作流本质相近，因为底层都是按章节这个小单元抽取”需要修正。

更准确的说法是：

```text
章节是调度、溯源、状态记录和增量重跑的基本单元。
LLM 实际处理单元可以是整章，也可以是章内场景片段。
```

也就是说，系统层面按章节管理任务，但模型层面不一定每次把整章全部塞给 LLM。

如果章节较短，可以整章抽取。

如果章节较长，应在章节内部按场景片段或段落窗口切分，再做章内汇总。

推荐结构：

```text
任务范围：当前章 / 多章 / 当前卷 / 全书
  ↓
调度单元：章节
  ↓
LLM 输入单元：整章或章内片段
  ↓
章内汇总：合并本章候选
  ↓
批次汇总：多章任务再做批次内去重和合并
```

因此，第一版不要把“章节”理解成唯一 LLM 输入单位，而应理解为最小可追踪业务单位。

## 3. 当前章、当前卷、全书的区别

当前章、当前卷、全书使用同一套大工作流，但复杂度不同。

当前章任务：

```text
单章读取
单章抽取
单章候选生成
单章审核
```

它适合第一版，用于调试字段、提示词、节点状态和候选审核。

当前卷任务：

```text
多章读取
逐章抽取
章内候选汇总
批次内去重
批次内合并
再与正式知识库比对
```

它比当前章多了批次内去重、进度调度、失败章节重试、批次级指标。

全书任务：

```text
多个卷 / 大量章节
长时间后台任务
更高重复率
更多冲突项
更复杂的运行恢复
更重的审核压力
```

全书不适合第一版直接开放。

## 4. 第一版开放范围

已确认：第一版只开放“当前章节”抽取。

第二版再做多章节批量处理。

推荐版本节奏：

```text
V1：当前章节抽取
V2：自定义章节范围，例如 1～5 章
V3：当前卷抽取
V4：全书抽取
```

原因：当前章节能最快验证完整闭环：读取正文、调用 LLM、生成候选、展示节点状态、进入审核、确认后写入 MongoDB。

如果第一版直接开放当前卷，会过早引入批次内去重、失败章节重试、长任务进度、候选爆炸等问题，容易影响第一个 Agent 的可用性验证。

## 5. JSON 中间态

已确认：第一版中间态先使用外部 JSON 文件。

中间态包括：

```text
运行记录
节点状态
候选新卡
候选更新
候选冲突
建议忽略
错误信息
提示词快照摘要或引用
```

外部 JSON 的作用：

```text
方便调试
方便回放
方便直接查看原始候选
不污染 MongoDB 主数据
适合第一版快速迭代字段
```

MongoDB 第一版只保存作者确认后的有效知识卡主数据。

后续如果候选量变大、审核页复杂、需要复杂筛选和分页，再考虑迁移到 MongoDB staging 集合。

## 6. 多智能体拆分粒度

已确认：第一版不做“每个类型一个完整 Agent 从头读正文”。

推荐方案是：

```text
通用抽取器 + 类型专家填表节点
```

核心思想：

```text
通用抽取器负责读正文、抽出候选实体和候选事实。
类型专家节点负责把候选按字段契约填成卡片草稿。
```

类型专家节点可以理解成“子 Agent”或“类型专属 LLM 节点”，但它们不重新从头读整章正文。它们接收的是通用抽取器给出的候选、必要摘录和少量上下文。

第一版推荐类型专家分组：

```text
角色填表节点
地点 / 势力 / 物品 / 事件填表节点
境界 / 功法 / 规则填表节点
统一合并与校验节点
```

后续如果发现某类抽取效果差，再拆出更细的类型专家。

## 7. 第一版 LangGraph 推荐主图

第一版主图应围绕“当前章节抽取”设计。

推荐节点：

```text
Start
  ↓
LoadChapterNode
  读取当前章节 Markdown、chapter_id、display_title、content_hash

  ↓
SegmentChapterNode
  判断章节长度；短章节整章处理，长章节切成场景片段

  ↓
GeneralExtractionNode（LLM）
  从整章或片段中抽出候选实体、候选事实、候选事件

  ↓
MergeChapterCandidatesNode
  合并章内多个片段的候选，去掉明显重复

  ↓
TypeDispatchNode
  分发到类型专家节点

  ↓
TypeExpertFillNode（LLM）
  按类型字段契约 v2 填充候选草稿

  ↓
NormalizeAndValidateNode
  规范字段、枚举、空值、chapter_id、来源摘录，并做 schema 校验

  ↓
MatchExistingKnowledgeNode
  与 MongoDB 已确认知识卡做名称、别名和摘要级匹配

  ↓
BuildReviewItemsNode
  生成候选新卡、候选更新、候选冲突、建议忽略

  ↓
WriteIntermediateJsonNode
  写入 JSON 中间态

  ↓
HumanReviewNode
  作者在智能体工作台审核

  ↓
WriteConfirmedKnowledgeNode
  作者确认后写入 MongoDB 有效知识库
```

第一版可以先让 HumanReviewNode 作为产品页面节点存在，不一定需要 LangGraph 在运行中真正等待前端 interrupt。实现上可以先把运行结果写入中间态，前端审核后再调用确认接口。

后续再升级为真正的可恢复 human-in-the-loop 图执行。

## 8. 哪些节点必须用 LLM

第一版真正必须用 LLM 的节点不多。

必须用 LLM：

```text
GeneralExtractionNode
TypeExpertFillNode
```

可选用 LLM：

```text
MatchExistingKnowledgeNode 中的模糊同名判断
候选冲突解释
候选更新说明
```

不需要 LLM：

```text
读取 Markdown
计算 content_hash
切分章节
生成 run_id
写 JSON 中间态
字段 schema 校验
枚举合法性检查
来源摘录裁剪
MongoDB 写入
状态流转
```

判断原则：

```text
写死更稳定的，不用 LLM。
需要语义理解和归纳的，用 LLM。
成本高但收益不明显的，先不用 LLM。
```

## 9. 确认后的知识卡是否立即可用

已确认：确认后的知识卡只要状态为有效，就立即进入写作页可参考知识库。

即使非必填字段缺失，也可以被结构化查询使用。

这很重要，因为后续 Agent 可能会再次发现同一张知识卡，并生成字段补充建议。

因此，正文知识沉淀 Agent 的后续运行不只会生成新卡，也会生成：

```text
更新已有卡字段
补充已有卡摘要
更新 last_seen_chapter_id
补充 current_realm_text
补充来源引用
提示可能冲突
```

## 10. 批量任务与中断恢复

已确认：当前章节任务第一版可以不做中断恢复。

第二版多章节任务至少需要支持失败章节重试。

第一版只做当前章节，因此最低要求是：

```text
单章运行失败时记录错误
允许重新运行当前章节
不需要复杂断点恢复
```

第二版多章节批量处理需要：

```text
每章独立状态
失败章节可重试
成功章节不重复跑
批次级运行记录
批次内候选去重
```

## 11. 待处理候选区域

已确认：第一版不新增单独主页面。

待处理候选放在智能体工作台当前 Agent 内。

推荐 Tab：

```text
运行任务
待处理候选
运行详情
评测指标
```

第一版前端展示可以先简化，但必须能验证功能闭环：

```text
能看到候选
能编辑候选
能确认入库
能废弃候选
能看到节点状态和基础指标
```

## 12. 我的推荐方案

综合用户目标、学习成本、实现风险和效果，第一版推荐方案是：

```text
当前章节抽取
外部 JSON 中间态
通用抽取器 + 类型专家填表节点
产品内自建运行记录和评测仪表盘
LangSmith 可选，不阻塞
确认后知识卡立即可被写作页结构化查询使用
待处理候选放在智能体工作台内
```

不推荐第一版直接做：

```text
当前卷抽取
全书抽取
每个类型一个完整 Agent
RAG
向量库
ES
Neo4j
GraphRAG
复杂恢复机制
自动写入有效知识库
```

## 13. 下一轮需要继续确认的问题

1. 当前章节抽取是否从写作页当前章节入口跳转到智能体工作台，还是只在智能体工作台里选择章节？

推荐：先只在智能体工作台里选择章节，避免写作页复杂化。

2. 第一版是否需要保存每次 LLM 调用的完整 prompt 和 response？

推荐：需要，用于调试和评测。

3. TypeExpertFillNode 是一个统一节点按类型传 schema，还是拆成三个专家节点？

推荐：拆成三个专家节点：角色专家、设定实体专家、事件规则专家。

4. 候选项是否需要置信度字段？

推荐：需要，但第一版只分 high / medium / low，不做复杂打分。

5. 候选确认时是否允许批量确认？

推荐：第一版不允许批量确认，只允许逐条确认，避免污染知识库。

6. 第一版是否需要自动生成事件卡？

推荐：需要，但只生成明确改变世界状态的关键事件，不把章节摘要拆成大量事件。

7. 第一版是否需要抽取境界卡？

推荐：需要，但境界卡生成要保守；无法判断 level_order 时进入待补字段。

8. 第一版是否需要抽取规则卡？

推荐：需要，但只抽取明确世界规则或例外，不抽推测。

9. 第一版是否需要自动匹配已有知识卡？

推荐：需要，但只做名称、别名、摘要相似的候选匹配；最终合并由作者确认。

10. 节点状态和指标第一版是否需要可视化图形？

推荐：第一版先用表格和状态标签，不做复杂曲线。曲线后续再加。
