# 连续执行总提示词

把本文件与任务包根目录的 `README.md`、`任务执行总规约.md`、`任务索引.json` 和当前阶段文件一起交给 Codex。

---

你正在 `qingluo201816/taichu` 仓库中执行太初通用 Agent 后续基础设施任务。

必须以当前本地仓库代码和 Git 历史为唯一事实锚点。任务包基线是 `master@7ad671f1f2558c39558c004e17b6d004b1a6b083`，但若当前 HEAD 更新，不得回退更成熟实现；先重新审计差异，再按当前代码调整落点。

执行规则：

1. 先读取当前仓库 `AGENTS.md`。
2. 涉及 docs 时读取 `docs/rule.md`。
3. 涉及 `.agents/skills/` 时读取 `.agents/skills/rule.md`。
4. 涉及前端时读取 `DESIGN.md` 和 `.agents/skills/taichu-ui-components/SKILL.md`。
5. 读取 `project_assets/readme.md`。
6. 运行任务包 `scripts/check_repository_baseline.py`。
7. 从阶段 00 开始，按 `任务索引.json` 的依赖顺序执行。
8. 每个阶段先核对任务包假设与当前代码，再实现。
9. 不重新实现已经存在的统一召回、Tool/子 Agent、Runtime、监控、评测和 checkpoint 第一版。
10. 不新增假 Tool、假 Agent、假生产 Embedding 或测试专用 Runtime。
11. 不设计多小说、`project_id`、多租户或跨小说检索。
12. 不让 `domain` 依赖 Agent、LangGraph、LLM、MCP、存储或向量技术。
13. Markdown 和 MongoDB 已确认知识是唯一事实源；所有索引、记忆、经验、运行和评测都是非事实。
14. AI 不直接写 Markdown 或 MongoDB；沿用真实写 Tool、作者授权、输入哈希、幂等和对账。
15. 所有用户可见文案中文，不做“器灵”等品牌化命名。
16. 使用 uv，不使用 pip/poetry/pipenv。
17. 每阶段完成代码、测试、评测、文档、回滚和自检。
18. 每阶段输出 `PASS`、`PASS_WITH_LIMITS`、`NO-GO`、`SKIPPED_BY_GATE` 或 `REBASE_REQUIRED`。
19. 阶段通过后自动进入下一阶段，不需要停下等待确认。
20. 实验门禁未达标时，不强行上线；记录 NO-GO，并继续与其无直接依赖的阶段。
21. 遇到事实边界、敏感信息、重复写、测试失败或基线假设失效等硬阻断，停止相关晋级并如实记录。
22. 不提供工时或日期估算。
23. 不提交、不推送、不开 PR，除非用户另行明确授权。

每个阶段结束必须写验收记录，至少包含：

- 当前 HEAD 和工作树。
- 完成任务 ID。
- 修改/新增/删除文件。
- Schema、配置、数据迁移。
- 测试命令和结果。
- 评测基线与当前结果。
- 安全、数据和架构自检。
- 回滚验证。
- 阶段结论。
- 下一阶段可用接口。

全量基础门禁：

```bash
uv run ruff check src tests
uv run mypy
uv run pytest -q
uv run python -m compileall -q src
git diff --check
```

涉及前端时：

```bash
cd web
npm run lint
npx tsc --noEmit
npm run test:general-agent
npm run test:llm
npm run build
```

涉及启动关键文件时，使用根 `start.bat` 验证固定端口 `localhost:3000` 和 `127.0.0.1:8000`。

现在开始执行当前依赖已满足的最早阶段。不要重新讨论任务拆分，不要向用户重复询问已经由任务包确定的目标。
