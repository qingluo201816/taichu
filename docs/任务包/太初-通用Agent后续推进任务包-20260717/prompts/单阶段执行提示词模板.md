# 单阶段执行提示词模板

请把 `{阶段文件}` 替换为实际路径后使用。

---

在 `qingluo201816/taichu` 当前仓库中，只执行任务包 `{阶段文件}`。

先读取：

- `AGENTS.md`
- `project_assets/readme.md`
- `docs/rule.md`（涉及 docs 时）
- `DESIGN.md` 与 `.agents/skills/taichu-ui-components/SKILL.md`（涉及 web 时）
- 任务包 `任务执行总规约.md`
- 所有前置阶段验收记录

要求：

1. 当前代码是事实，任务包基线路径仅作导航。
2. 先做阶段前置条件和基线漂移检查。
3. 完成阶段内全部任务 ID，除非某项被真实代码吸收或被硬门禁阻断。
4. 已实现能力只做回归与缺口，不从零重写。
5. 不新增假生产能力。
6. 完成单元、集成、兼容、失败、数据边界、观测和评测。
7. 完成全量门禁和必要的前端/固定端口验证。
8. 输出阶段验收记录和 `PASS`、`PASS_WITH_LIMITS`、`NO-GO`、`SKIPPED_BY_GATE` 或 `REBASE_REQUIRED`。
9. 不提交、不推送、不开 PR。
10. 不提供工时估算。
