# 临时架构资料说明

> 更新日期：2026-07-10

本目录保存尚未完全落地、仍需与当前代码核对，或同时包含未来规划的架构资料。

- 这里的文档不是当前实现的唯一事实源。
- 文档若包含代码基线或提交哈希，只代表该时间点的观察结果。
- 开发前必须同时检查 `src/`、`web/`、`project_assets/readme.md` 和根目录 `AGENTS.md`。
- 架构确认并落地后，应更新根目录仓库地图，并把过期方案移入 `docs/历史/` 或删除。
